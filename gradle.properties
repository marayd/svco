# Done to increase the memory available to gradle.
org.gradle.jvmargs=-Xmx4G
org.gradle.java.home=C:/Program Files/Eclipse Adoptium/jdk-25.0.2.10-hotspot
org.gradle.parallel=true

# IntelliJ IDEA is not yet fully compatible with configuration cache, see: https://github.com/FabricMC/fabric-loom/issues/1349
org.gradle.configuration-cache=false

# Shared tooling versions; per-Minecraft values live in versions/<mc>/gradle.properties
loader_version=0.18.4
loom_version=1.17-SNAPSHOT

# Mod Properties
mod_version=1.0.1
maven_group=com.example

# Modrinth publishing (see .github/workflows/release.yml).
# The release workflow overrides mod_version/modrinth_version_type from the tag.
mod_display_name=Simple Voice Chat Online
modrinth_project_id=I2sL90Jf
modrinth_version_type=release
